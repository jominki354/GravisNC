<Window x:Class="GCode.App.WPF.Views.SettingsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="설정" Height="500" Width="700"
        WindowStartupLocation="CenterOwner"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        ResizeMode="NoResize">
        
    <WindowChrome.WindowChrome>
        <WindowChrome CaptionHeight="40" 
                      ResizeBorderThickness="5" 
                      CornerRadius="10" 
                      GlassFrameThickness="0"/>
    </WindowChrome.WindowChrome>

    <Window.Resources>
        <!-- Text Style -->
        <Style TargetType="TextBlock">
            <Setter Property="Foreground" Value="#CCCCCC"/>
            <Setter Property="FontFamily" Value="Segoe UI"/>
            <Setter Property="FontSize" Value="13"/>
        </Style>
        
        <!-- Section Header Style -->
        <Style x:Key="SectionHeader" TargetType="TextBlock">
            <Setter Property="FontSize" Value="18"/>
            <Setter Property="Foreground" Value="#CCCCCC"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Margin" Value="0,0,0,15"/>
        </Style>

        <!-- Input Label Style -->
        <Style x:Key="InputLabel" TargetType="TextBlock">
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="Foreground" Value="#CCCCCC"/>
            <Setter Property="Margin" Value="0,0,0,5"/>
        </Style>
        
        <!-- Cancel Button Style Override (Secondary) -->
        <Style x:Key="SecondaryButtonStyle" TargetType="Button" BasedOn="{StaticResource {x:Type Button}}">
            <Setter Property="Background" Value="#3E3E42"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                         <Border x:Name="Bd" Background="{TemplateBinding Background}" CornerRadius="2">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" Margin="{TemplateBinding Padding}"/>
                        </Border>
                        <ControlTemplate.Triggers>
                             <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Bd" Property="Background" Value="#4E4E52"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="Bd" Property="Background" Value="#2D2D30"/> 
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Window Control Button Style -->
        <Style x:Key="WindowButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="#CCCCCC"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="Bd" Background="{TemplateBinding Background}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Bd" Property="Background" Value="#3E3E40"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style x:Key="WindowCloseButtonStyle" TargetType="Button" BasedOn="{StaticResource WindowButtonStyle}">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="Bd" Background="{TemplateBinding Background}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Bd" Property="Background" Value="#E81123"/>
                                <Setter Property="Foreground" Value="White"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>
    
    <Border Background="#1E1E1E" CornerRadius="10" BorderBrush="#333333" BorderThickness="1">
       <Border.Effect>
            <DropShadowEffect BlurRadius="20" ShadowDepth="5" Opacity="0.5"/>
        </Border.Effect>
        
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="40"/> <!-- Title Bar -->
                <RowDefinition Height="*"/>    <!-- Content -->
            </Grid.RowDefinitions>
            
            <!-- Title Bar -->
            <Border Grid.Row="0" Background="#2D2D30" CornerRadius="10,10,0,0" MouseLeftButtonDown="TitleBar_MouseDown">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    
                    <TextBlock Grid.Column="0" Text="설정" VerticalAlignment="Center" Margin="15,0,0,0" FontWeight="SemiBold"/>
                    
                    <StackPanel Grid.Column="2" Orientation="Horizontal" WindowChrome.IsHitTestVisibleInChrome="True">
                        <Button Content="✕" Width="46" Height="40" Style="{StaticResource WindowCloseButtonStyle}" Click="BtnCancel_Click"/>
                    </StackPanel>
                </Grid>
            </Border>

            <!-- Main Content -->
            <Grid Grid.Row="1">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="200"/> <!-- Sidebar -->
                    <ColumnDefinition Width="*"/>   <!-- Content -->
                </Grid.ColumnDefinitions>

                <!-- Sidebar -->
                <Border Grid.Column="0" Background="#252526" BorderBrush="#333333" BorderThickness="0,0,1,0">
                    <StackPanel Margin="0">
                        <TextBlock Text="설정 목록" FontSize="11" Foreground="#969696" Margin="20,15,0,10" FontWeight="Bold"/>
                        <ListBox Background="Transparent" BorderThickness="0" SelectedIndex="0">
                            <ListBoxItem Content="텍스트 편집기" Foreground="#FFFFFF" Padding="20,8" IsSelected="True"/>
                            <ListBoxItem Content="G-Code (준비중)" Foreground="#858585" Padding="20,8" IsHitTestVisible="False"/>
                        </ListBox>
                    </StackPanel>
                </Border>

                <!-- Content Area -->
                <Grid Grid.Column="1" Background="#1E1E1E">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/> <!-- Search Header -->
                        <RowDefinition Height="*"/>    <!-- Settings List -->
                    </Grid.RowDefinitions>

                    <!-- Search Settings Bar (VS Code Style) -->
                    <Border Grid.Row="0" Padding="30,20,30,10">
                        <TextBox Tag="설정 검색..." Height="28" FontSize="13" VerticalContentAlignment="Center"/>
                    </Border>

                    <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
                        <StackPanel Margin="30,0,30,30">
                            <TextBlock Text="텍스트 편집기" Style="{StaticResource SectionHeader}" Margin="0,10,0,20"/>
                            
                            <!-- Font Family (Searchable) -->
                            <StackPanel Margin="0,0,0,20">
                                <TextBlock Text="글꼴 (Font Family)" Style="{StaticResource InputLabel}"/>
                                <TextBlock Text="편집기에서 사용할 글꼴을 제어합니다." Foreground="#969696" FontSize="12" Margin="0,0,0,8"/>
                                <ComboBox x:Name="ComboFontFamily" Width="350" HorizontalAlignment="Left" 
                                          IsEditable="True" IsTextSearchEnabled="True"/>
                            </StackPanel>

                            <!-- Font Size -->
                            <StackPanel Margin="0,0,0,20">
                                <TextBlock Text="글꼴 크기 (Font Size)" Style="{StaticResource InputLabel}"/>
                                <TextBlock Text="픽셀 단위의 글꼴 크기입니다." Foreground="#969696" FontSize="12" Margin="0,0,0,8"/>
                                <ComboBox x:Name="ComboFontSize" Width="120" HorizontalAlignment="Left"/>
                            </StackPanel>

                            <!-- Font Weight -->
                            <StackPanel Margin="0,0,0,20">
                                <TextBlock Text="글꼴 굵기 (Font Weight)" Style="{StaticResource InputLabel}"/>
                               <TextBlock Text="글자의 굵기를 설정합니다." Foreground="#969696" FontSize="12" Margin="0,0,0,8"/>
                                <ComboBox x:Name="ComboFontWeight" Width="350" HorizontalAlignment="Left">
                                    <ComboBoxItem Content="Normal (일반)"/>
                                    <ComboBoxItem Content="Bold (굵게)"/>
                                </ComboBox>
                            </StackPanel>

                            <Separator Background="#3E3E42" Margin="0,20,0,20"/>
                        
                        <!-- Buttons -->
                        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                            <Button x:Name="BtnSave" Content="적용 및 저장" Click="BtnSave_Click" Margin="0,0,10,0"/>
                            <Button x:Name="BtnCancel" Content="취소" Click="BtnCancel_Click" Style="{StaticResource SecondaryButtonStyle}"/>
                        </StackPanel>
                    </StackPanel>
                </ScrollViewer>
            </Grid>
        </Grid>
    </Grid>
    </Border>
</Window>
